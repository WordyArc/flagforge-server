product: jvm/app

apply:
  - ../../common.module-template.yaml
  - ../../spring.module-template.yaml

dependencies:
  - $libs.spring.kafka
  - $libs.spring.boot.kafka

settings:
  kotlin:
    optIns: [ kotlin.uuid.ExperimentalUuidApi ]